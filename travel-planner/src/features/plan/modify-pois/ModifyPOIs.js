export default function ModifyPOIs() {
    const rootClassName = ["modify-pois"].join(" ");

    return (
        <div className={rootClassName}>
            <h1>ModifyPOIs</h1>
        </div>
    );
}